{
  "name": "birthday-slideshow",
  "version": "1.0.0",
  "description": "Birthday Slideshow App",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run preview"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
